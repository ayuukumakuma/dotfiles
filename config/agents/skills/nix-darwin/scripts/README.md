# nix-darwin Skill Scripts

## search_docs.py

Searches Markdown files under `references/` and prints matching snippets.

```bash
python scripts/search_docs.py "homebrew cleanup"
python scripts/search_docs.py "system.defaults dock" --json
```

